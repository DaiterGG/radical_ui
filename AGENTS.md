functions inside functions and inline functions are strictly prohibited, define a normal function and pass all parameters; with the exception of actions_poll.lua
do not omit errors like: self.is_on = is_on == true, if method called incorrectly, program should crash
